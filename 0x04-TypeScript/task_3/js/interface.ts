// Defining the RowID type as a number
export type RowID = number;

// Defining the RowElement interface with the specified fields
export interface RowElement {
    firstName: string
    lastName: string
    age?: number
}