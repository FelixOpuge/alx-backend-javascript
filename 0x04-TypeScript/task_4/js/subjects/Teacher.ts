//Defining the Subject namespace
namespace Subjects {
    //Defining the Teacher Interface within the Subjects namespace
    export interface Teacher {
        firstName: string;
        lastName: string;
    }
}